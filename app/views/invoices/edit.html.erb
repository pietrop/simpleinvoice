
<div class="well">
<fieldset>
  <legend>Edit Invoice Details </legend>

<%= form_for [@user , @invoice] do |f| %>
<div class="form-group">
    <%= f.label :number %><br>
    <%= f.text_field :number, class: 'form-control' %>
  </div>
<div class="form-group">
    <%= f.label :date %><br>
    <%= f.date_field :date, class: 'form-control' %> </p>
  </div>
 
 <%= link_to 'Back', user_invoice_path(@user, @invoice),  class: "btn btn-primary" %>
  <%= f.button 'Create' , class: 'btn btn-primary' %>

 <% end %>
</fieldset>
</div>

</div>
